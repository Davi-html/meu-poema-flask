:root {
  --nav-bg: #D9D9D9;
  --btn-color: #6BA229;
  --bg: #636161;
  --post: rgba(41, 40, 40, 0.8);
  --login-color: #8B8B8B;
  --bg-title-login: #262626;
  --btn-message-color: #297ca2;
  --btn-report-color: #A2292B ;
  --btn-edit-color: #ce5e17;
}

.body-base {
  display: grid;
  grid-template-columns: 221px 800px 1fr;
}

.body-base {
  background-color: var(--bg);
}

.sidebar {
  width: 221px;
  height: 100%;
  background-color: var(--nav-bg);
  position: fixed;

  .top {
    height: 140px;
    
    display: flex;
    flex-direction: column;
    align-items: center;
    
    img {
      margin-top: 1.5rem;
    }
  }

  hr {
    border: 1px solid #000;
  }

  .nav {
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 1rem;

    a {
      font-size: 16px;
      font-weight: 400;
      line-height: 24px;
      letter-spacing: 0em;
      text-align: center;
      padding: 1rem;
      color: black;
    }
  }

  .suporte {
    width: 221px;
    height: 154px;
    position: absolute;
    bottom: 0;

    button {
      width: 100%;
      height: 100%;
      border: none;
      background-color: var(--btn-bg);
      color: #fff;
      font-size: 16px;
      font-weight: 400;
      line-height: 24px;
      letter-spacing: 0em;
      text-align: center;
    }
  }

}

.fy {
  display: flex;
  
  align-items: center;
  flex-direction: column;
  margin: 30px 0;

  height: auto;
  width: 100%;

  .input-search {
    width: 90%;
    height: 69px;
    border: none;
    border-radius: 10px;
    padding: 1rem;
    margin-bottom: 20px;
  }

  .create-poem {
    width: 90%;
    height: 69px;
    display: flex;
    gap: 30px;

    padding: 1rem;
    border-radius: 10px;
    background-color: var(--post);
  
    input {
      width: 100%;
      border: 2px solid var(--nav-bg);
      border-radius: 10px;
      color: #fff;
      background-color: transparent;
      font-size: 16px;
      text-align: left;
      padding: 15px;
    }
  }

  .post {
    width: 90%;
    margin-top: 20px;
    border-radius: 20px;
    padding: 20px;
    background-color: var(--post);

    .postHeader {
      display: flex;
      height: 100%;
    
      .img-profile {
        width: 80px;
        height: 80px;
        border-radius: 10px;
        border-radius: 50%;
      }
    }

    .info-profile {
      display: flex;
      flex-direction: column;
      margin-left: 10px;

      .name-profile {
        font-size: 16px;
        font-weight: 600;
        line-height: 24px;
        letter-spacing: 0em;
        text-align: left;
        color: #fff;
      }

      .time-post {
        color: var(--login-color);
        font-size: 16px;
      }
    }

    nav {
      display: flex;
      gap: 10px;
      margin-left: auto;
      align-items: center;

      .div-settings {
        width: 40px;
        height: 36px;
        border-radius: 10px;
        color: #fff;
        font-weight: bolder;
        cursor: pointer;
        background-color: var(--login-color);
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .bookmark {
        cursor: pointer;
        width: 40px;
      }
    }
  }

}

.suporte {
    background-color: var(--btn-color);
}

.rank {
    margin: 30px 0px;
    display: flex;
    justify-content: center;
    color: #fff;

    .title {
        display: flex;
        justify-content: space-between;
        margin-bottom: 10px;
    }

    .followers {
        display: flex;
        flex-direction: column;
        gap: 10px;
        background-color: var(--post);
        border-radius: 20px;
        padding: 20px;
        width: 293px;
        height: auto;
        margin-bottom: 48px;

        .userRanking {
            display: flex;
            flex-direction: column;

            .user {
                display: flex;
                gap: 10px;
                align-items: center;
            }
        }

        .userRanking, div{
            display: flex;
            gap: 20px;

            p {
                margin: 0;
            }
        }
    }

    .img-rank {
        width:30px;
        height: 30px;
        border-radius: 50%;
        object-fit: cover;
    }

    .notifications {
        display: flex;
        flex-direction: column;
        gap: 10px;
        background-color: var(--post);
        border-radius: 20px;
        padding: 20px;
        width: 293px;
        height: auto;
        margin-bottom: 48px;
        text-align: center;

        #title-notification {
            font-size: 20px;
        }

        .bi-bell {
            font-size: 69px;
        }

        #text-notification {
            text-align: center;
        }
    }
}

.signup, .signin {
    display: flex;
    flex-direction: column;
    gap: 10px;
    background-color: #fff;
    border-radius: 20px;
    width: 476px;
    height: auto;
    margin: auto;
    border-radius: 16px;
    text-align: center;


    .title {
        background-color: var(--bg-title-login);
        display: flex;
        justify-content: center;
        align-items: center;
        height: 105px;
        color: #fff;
        border-radius: 16px 16px 0 0;
        gap: 13px;
    }

    .div-input-login {
        display: flex;
        flex-direction: column;
        padding: 20px;
        text-align: left;
        gap: 5px;

        #submit {
            margin-top: 20px;
            background-color: var(--btn-color);
            color: #fff;
            border: none;
            padding: 10px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: bold;
        }
    }

    .social-accounts {
        display: flex;
        flex-direction: column;
        padding: 20px;
        justify-content: center;
        gap: 10px;


        button {
            background-color: transparent;
            padding: 5px 0;
            border: 2px solid var(--nav-bg);
        }
    }

    .div-email-name {
        display: flex;
        justify-content: space-between;
        gap: 20px;
    }

    input {
        width: 100%;
        padding: 8px;
        border: 2px solid var(--nav-bg);
        border-radius: 5px;
    }

    .description #description-title {
        font-size: 24px;
        font-weight: bold;
        line-height: 24px;
        letter-spacing: 0em;
        text-align: center;
        color: black;
    }

    .description #description-text {
        color: var(--login-color);
        font-size: 16px;
        line-height: 24px;
        letter-spacing: 0em;
        text-align: center;
    }

    .link {
        font-size: 16px;
        line-height: 24px;
        letter-spacing: 0em;
        text-align: center;

        a {
            color: var(--btn-color);
            font-weight: bold;
        }
    }
}

.notification-page {
    padding: 20px;
    color: white;
    margin-left: 70px;
    background-color: var(--post);
}

.div-lembrar-senha {
    display: flex;
    align-items: center;
    margin-top: 15px;
    gap: 10px;

}
.div-lembrar-senha input {
        width: fit-content;
}

.div-lembrar-senha label {
    margin-bottom: 0;
}

.profile, .config, .messagePage {
    width: 689px;
    height: 940px;
    margin: 40px auto;
    background-color: var(--post);
    color: white;
    padding: 30px;
}

.profile-header, .edit-profile-header {
    display: flex;
    gap: 50px;
    align-items: center;
    padding: 20px;
    border-bottom: 1px solid var(--login-color);

    .profile-name {
        #name {
            font-size: 20px;
            font-weight: bold;
            line-height: 24px;
            letter-spacing: 0em;
            text-align: center;
            margin-top: 15px;
        }

        #username {
            font-size: 15px;
            letter-spacing: 0em;
            text-align: center;
            color: var(--login-color);
        }
    }

    .profile-image img {
        width: 146px;
        height: 146px;
        border-radius: 50%;
    }

    .profile-info{
        display: flex;
        flex-direction: column;
        gap: 20px;

        .followers {
            display: flex;
            gap: 30px;
        }

        .profile-btn-info button, input {
            border-radius: 20px;
            background-color: var(--btn-color);
            border: none;
            padding: 10px;
        }

        .profile-btn-info {
            display: flex;
            gap: 20px;

            #follow {
                background-color: var(--btn-color);
            }

            #message {
                background-color: var(--btn-message-color);
            }

            #edit {
                background-color: var(--btn-edit-color);
            }
        }
    }
}

ul.flashes {
    list-style: none;
    display: flex;
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;

    li {
        padding: 10px;
    }
}

.config-session-aparencia, .config-session-conta, .config-session-info{
    h1 {
        color: var(--btn-color);
    }

    .instagram {
        text-decoration: none;
        color: white;
        font-size: 40px;
    }
}

#excluir-conta a{
    color: var(--btn-report-color);
}

.input-remove-config {
    display: flex;
    width: 220px;
    justify-content: space-between;
    align-items: center;

    p {
        margin: 0;
    }

     input {
        min-width: 20px;
        min-height: 20px;
    }
}

.avatar-profile {
    object-fit: cover;
}

.edit-profile {
    #name, #email {
        background-color: transparent;
        outline: none;
    }

    #div-input-name, #div-input-email{
        display: flex;
        flex-direction: column;

        margin-bottom: 20px;

        input {
            width: 100%;
            padding: 8px;
            border: 2px solid var(--nav-bg);
            border-radius: 5px;
        }
    }

}

.btn-edit-profile {
    background-color: var(--btn-color);
    border: none;
    padding: 10px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: bold;
}

.edit-profile-header {
    display: flex;
    justify-content: center;
    margin-bottom: 10px;

    .profile-image {
        display: flex;
        flex-direction: column;
        gap: 10px;
        align-items: center;
    }
}

.chat-container {
    max-width: 800px;
    margin: 20px auto;
    border-radius: 10px;
    box-shadow: 0 0 15px rgba(0,0,0,0.1);
    overflow: hidden;
}
.chat-header {
    background-color: #0d6efd;
    color: white;
    padding: 15px;
    display: flex;
    align-items: center;
}
.chat-messages {
    height: 500px;
    overflow-y: auto;
    padding: 20px;
    background-color: #f8f9fa;
    display: flex;
    flex-direction: column;
}
.message {
    margin-bottom: 15px;
    max-width: 70%;
    animation: fadeIn 0.3s ease-in-out;
}
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}
.received {
    align-self: flex-start;
    background-color: white;
    padding: 10px 15px;
    border-radius: 0 10px 10px 10px;
    box-shadow: 0 1px 1px rgba(0,0,0,0.1);
}
.sent {
    align-self: flex-end;
    background-color: #d1e7dd;
    padding: 10px 15px;
    border-radius: 10px 0 10px 10px;
    box-shadow: 0 1px 1px rgba(0,0,0,0.1);
}
.message-time {
    font-size: 0.8rem;
    color: #6c757d;
    margin-top: 5px;
}
.chat-input {
    padding: 15px;
    background-color: white;
    border-top: 1px solid #dee2e6;
    display: flex;
}
.typing-indicator {
    font-style: italic;
    color: #6c757d;
    padding: 5px 15px;
    font-size: 0.9rem;
}

.messagePage-messages {
    display: flex;
    margin-top: 20px;

    .messagePage-info {
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

}