{% extends 'base.html' %}

{% block body %}
    <div class="notification-page">
        <div class="notification-item">

            <div class="notification-content">
                {% for notification in notification %}
                    <p>{{ notification.user.username }} {{ notification.message }}</p>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}